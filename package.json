{
  "name": "snowwhite",
  "version": "0.1.0",
  "description": "Modules for MagicMirror",
  "scripts": {
    "release": "npm pack && mkdir dist && mv snowwhite-0.1.0.tgz dist/",
    "test": "npm i nodeunit-mock && grunt",
    "format": "prettier --write *"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YeetCodes/SnowWhite.git"
  },
  "keywords": [
    "magic",
    "mirror",
    "modules"
  ],
  "dependencies": {
    "grunt": "latest",
    "grunt-contrib-nodeunit": "latest",
    "grunt-jslint": "latest",
    "jsdom": "^11.6.2",
    "load-grunt-config": "latest",
    "nodeunit-mock": "latest",
    "prettier": "^2.0.5"
  },
  "devDependencies": {
    "eslint": "^6.8.0"
  }
}
